// subtext.js (LEGACY - DEPRECATED)
// This file is kept temporarily for backward compatibility.
// New structured meta lives in lib/taskMeta.js (buildTaskMeta).
// TODO: Remove after confirming no external imports rely on this.
/**
 * @deprecated Use buildTaskMeta(task) instead.
 */
export function formatTaskSubtext(_task) {
  return '';
}
